function distinct(a) {
  
  
  let results = [];
  

   for (let i = 0; i < a.length; i++){
     if(results.indexOf(a[i])=== -1){
       
      
       results.push(a[i]);
     }
   }
  return results ;  // Return the array without duplicates
}