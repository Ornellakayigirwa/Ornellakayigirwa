function pigIt(str){
     return str.split(' ')
     .map(word=>{
    if(word.toUpperCase() === word.toLowerCase()){
        return word
    }
         return word.slice(1)+word[0]+'ay'
     }).join(' ')
}