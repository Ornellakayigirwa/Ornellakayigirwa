function spinWords(string){
  
  let str = string.split(' ')
  
  let words = str.map(word=>{
    
    if(word.length >= 5){
      
   return  word.split('').reverse().join('')
      
    } else{
      
      return word
    }
  })
  
  return words.join(' ')
}